# aurora-platform-storage

Native Aurora package persistence. The MVP writer emits ZIP packages with `document.json`, `styles.json`, `metadata.json`, `assets/`, `history/`, and `embeddings/`.

This module also owns local settings, recent document tracking, user data paths, and timestamped autosave snapshots.
