# Application icons

Drop platform icons here. `scripts/package.py` looks for:

- `app.ico` for Windows
- `app.icns` for macOS
- `app.png` for Linux / AppImage

If a file is missing the packaging script silently builds without an icon.
