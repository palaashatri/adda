# Themes

Place additional `customtkinter` JSON color themes here. The app currently uses
the built-in `blue` dark theme (see `ui/theme.py`); to load a custom theme,
pass its absolute path to `ctk.set_default_color_theme(...)` instead.
